# 开发交接（当前阶段）

更新时间：2026-02-27 18:55

## 1. 结论先行
- `回测验证与任务编排（run_type 生产隔离）` 已验收通过，可切阶段。
- `AI增强（参数优化建议 -> 配置版本化）` 已完成并通过 10 轮稳定性回归。
- `UI美化（搜索/分类/可读性优化）` 已完成并已切阶段（作为回归基线）。
- `发布后运行观察与收官验收` 已完成并转为基线。
- `收官归档与维护交接` 已完成并转为基线。
- 当前阶段锁：`维护模式（回归巡检）`，首轮巡检门槛 4/4 已通过。

## 2. 已完成阶段证据（已归档）
1. `API 拉取 -> 数据质量门控`（上一阶段）
- 最新通过报告：`reports/quality_gate_acceptance_20260226_214608.json`
- 关键结论：放行场景 `publish_allowed=true -> scoring 200`；阻断场景 `publish_allowed=false -> scoring 400`

2. `回测验证与任务编排（run_type 生产隔离）`（刚完成阶段）
- 最新通过报告：`reports/backtest_stage_acceptance_20260226_214600.json`
- 关键结论：
  - `POST /api/backtest/run` 返回 `run_id`
  - 任务终态可达 `completed`
  - 结果接口支持 `run_id` 过滤
  - `trend_day_consistency` 通过（抽样不一致数 0）
  - `run_type=prod/backtest` 查询可区分

## 3. 当前阶段锁（必须遵守）
- 当前阶段：`维护模式（回归巡检）`
- 当前状态：`进行中（4/4 已通过，进入持续巡检）`
- 本阶段未完成前，不跨阶段推进新能力开发。

## 4. 当前阶段目标（维护模式-回归巡检）
1. 阶段锁切换与可见化
- 首页阶段卡新增第 12 步并切到 `维护模式（回归巡检）`

2. 维护巡检检查面板
- 首页新增“维护模式（回归巡检）”面板
- 最小检查项：同步心跳/质量门控/回测终态/配置回灌

3. 验收收口
- 固化命令化检查与维护巡检提示，供非代码验收
- 现状：第 1~4 项已完成，最新报告：`reports/maintenance_mode_acceptance_20260227_181325.json`

## 5. 上一阶段目标（AI增强，已完成）
对应流程图链路：`回测报告 -> 参数优化建议 -> 配置保存（版本化） -> 回灌评分`

1. 参数建议可追踪
- 每次建议要绑定来源 `run_id`
- 建议内容可落地为可保存参数集

2. 配置版本化可回滚
- 参数保存形成版本记录（至少支持查看与切换）
- 切换版本后可触发新一轮评分/回测验证

3. Web 可见验收
- 首页“链路进度（阶段锁）”第 6 步显示进行中
- 设置页可看到“当前参数版本”与“最近应用来源（run_id）”

4. 阶段验收固化
- 新增本阶段验收文档
- 增加最小回归脚本（参数保存/切换/回灌）

## 6. 已有能力（可直接用）
- 回测任务接口：
  - `POST /api/backtest/run`
  - `GET /api/backtest/jobs`
  - `GET /api/backtest/jobs/{run_id}`
  - `POST /api/backtest/jobs/{run_id}/cancel`
  - `POST /api/backtest/jobs/{run_id}/retry`
- 回测结果接口：
  - `GET /api/backtest/results`
  - `GET /api/backtest/results/{target_date}`
  - 支持 `run_id` 过滤：`/api/backtest/results?run_id=...`
- 评分结果隔离字段：
  - `predictions.run_type`
  - `predictions.run_id`

## 7. 上一阶段最小实施顺序（AI增强，已完成）
1. 设计参数建议的数据结构（绑定 `run_id` + 参数快照）。
2. 落地配置版本表/版本接口（保存、查询、应用）。
3. 接通“应用版本 -> 触发评分/回测”的最小闭环。
4. 补齐阶段验收脚本与文档。

## 8. 代码入口（优先阅读）
- `server/core/backtest.py`
- `server/core/scoring.py`
- `server/routes/backtest_routes.py`
- `server/routes/config_routes.py`
- `frontend/app.js`

## 9. 回归基线（必须保留）
- 数据质量门控：
  - `scripts/run_quality_gate_acceptance.ps1`
  - `scripts/run_quality_gate_stability.ps1`
- 回测阶段：
  - `scripts/run_backtest_stage_acceptance.ps1`
  - `scripts/run_backtest_stage_stability.ps1`

## 10. 本轮新增验收证据
- API 验收：`reports/backtest_stage_acceptance_20260226_214600.json`
- 基线回归：`reports/quality_gate_acceptance_20260226_214608.json`
- Web 验收截图：
  - `pipeline-stage-backtest-runid.png`
  - `backtest-consistency-check-pass.png`
- UI阶段新增截图：
  - `reports/ui_polish_acceptance_desktop_wide_20260227.png`
  - `reports/ui_polish_acceptance_mobile_20260227.png`
- 监控阶段验收文档（基线）：
  - `docs/阶段验收-监控与验收可见性固化.md`
- 发布阶段命令化验收（新增）：
  - 脚本：`scripts/run_release_stage_acceptance.ps1`
  - 报告：`reports/release_stage_acceptance_20260227_154957.json`
  - 截图：
    - `reports/release_stage_panel_20260227.png`
    - `reports/release_check_panel_20260227.png`
    - `reports/release_runbook_panel_20260227.png`
  - 运行手册：
    - `docs/发布运行手册-发布准备与运行保障.md`
- 收官归档阶段验收（基线）：
  - 脚本：`scripts/run_closure_archive_acceptance.ps1`
  - 报告：`reports/closure_archive_acceptance_20260227_175746.json`
- 维护模式阶段验收（当前）：
  - 脚本：`scripts/run_maintenance_mode_acceptance.ps1`
  - 连续巡检：`scripts/run_maintenance_mode_stability.ps1`
  - 一键入口：`check_maintenance_mode_stability.bat`
  - 报告：`reports/maintenance_mode_acceptance_20260227_181325.json`
  - 稳定性：`reports/maintenance_mode_stability_20260227_182923.json`

## 附录 A：AI增强阶段进展（2026-02-26 22:20）
1. 已完成：参数建议可追踪（R）
- 接口：
  - `POST /api/ai/optimization/suggestions/generate`
  - `GET /api/ai/optimization/suggestions`
- 要点：
  - 建议记录绑定 `run_id`
  - 建议含 `summary/current_params/suggested_params/reasoning`

2. 已完成：配置版本化核心能力（S 的主体）
- 新增表：
  - `algo_config_versions`
- 新增接口：
  - `GET /api/config/versions`
  - `GET /api/config/versioning/status`
  - `POST /api/config/versions/save`
  - `POST /api/config/versions/{id}/apply`
- 前端可见：
  - 设置页新增“算法配置版本化（保存/查询/应用）”
  - 首页阶段卡第 6 步会显示“配置版本 Vx 已应用（run_id=...）”

3. 当前未完成（S -> K 闭环）
- 应用版本后自动触发“回灌评分/回测”的一键链路尚未接通（当前为手动触发）
- 阶段锁仍保持在 `AI增强`

### A1. S->K 闭环进展（2026-02-26 23:56）
1. 新增能力：应用版本时可一键回灌
- 接口：`POST /api/config/versions/{id}/apply`
- 新增参数：
  - `run_scoring`（bool）
  - `run_backtest`（bool）
  - `backtest_days`（int）
- 返回增加：
  - `scoring`（`ok/blocked` + run_id）
  - `backtest`（queued run_id）

2. 状态语义增强
- 版本状态新增：`refeeded`（表示已完成“应用版本 + 回灌评分”）
- 首页阶段卡第 6 步文案已区分：
  - `已应用 -> 待回灌`
  - `已回灌评分 -> 待观察效果`

3. 本轮验收证据
- `reports/ai_enhancement_acceptance_20260226_235621.json`
- 关键结果：
  - `version_status=refeeded`
  - `scoring_status=ok`
  - `backtest_status=completed`

### A2. 状态更正（2026-02-26 23:58）
- 第 10 节中“当前未完成（S -> K 闭环）”已过期。
- 以第 11 节为准：`S -> K` 已具备一键回灌能力，当前进入稳定性回归与验收固化。

### A3. 新增阶段回归脚本
- `scripts/run_ai_enhancement_acceptance.ps1`
- 示例：
  - `powershell -ExecutionPolicy Bypass -File scripts/run_ai_enhancement_acceptance.ps1 -BaseUrl http://127.0.0.1:8000 -RunBacktest -BacktestDays 20`
- 最近产物：
  - `reports/ai_enhancement_acceptance_20260226_235926.json`

### A4. AI增强阶段（稳定性收口）
- 新增稳定性脚本：`scripts/run_ai_enhancement_stability.ps1`
- 新增阶段验收文档：`docs/阶段验收-AI增强-参数优化建议与配置版本化.md`
- 目标：将 `S -> K` 闭环固化为可重复回归并形成切阶段证据。

### A5. AI增强稳定性回归结果（2026-02-27 01:47）
1. 首轮 10 轮回归未通过（用于复现问题）
- 报告：`reports/ai_enhancement_stability_20260227_014400.json`
- 现象：`apply_status=ok`、`backtest_status=completed`，但 `scoring_status=blocked`，`version_status=applied`
- 结论：freshness 门控触发，导致评分回灌被阻断

2. 修复项
- 稳定性脚本新增预刷新能力：`PreRefresh`（默认 `true`）
- 执行回归前先调用 `POST /api/sectors/refresh`，避免因旧同步时间导致误阻断

3. 修复后 10 轮回归通过
- 报告：`reports/ai_enhancement_stability_20260227_014625.json`
- 日志：`reports/ai_enhancement_stability_20260227_014625.log`
- 结果：`all_passed=true`、`passed_count=10`、`failed_count=0`

## 15. 当前阶段结论（对齐流程图）
- 已完成：`A -> S`（含 `S -> K` 一键回灌闭环）
- 进行中：`T`（监控与验收信息可见性固化）
- 阶段锁：已切换到 `监控与验收可见性固化（T）` 并开始开发。

## 16. UI美化阶段收口（2026-02-27 03:25）
1. 阶段锁切换
- 前端默认阶段锁改为 `ui_polish`
- 阶段卡在切锁后，`AI增强` 显示“已完成”，避免误判为阻塞

2. 可见功能落地
- 首页“预测排行”新增筛选与排序能力：
  - 搜索
  - 类型筛选
  - 得分区间筛选（强势/观察/弱势）
  - 排序切换（Rank/得分/日涨幅/偏差）
  - 仅关注切换
  - 一键重置
- 新增筛选结果摘要：`当前结果 X 条 · 行业 Y · 概念 Z`

3. 文案可读性修复
- 修复首页、系统设置、弹窗、详情面板关键中文文案乱码
- 修复部分状态文案与兜底文案（如“状态/缺失/加载中/失败提示”）

4. 相关代码
- `frontend/app.js`
- `frontend/index.html`
- `frontend/style.css`
